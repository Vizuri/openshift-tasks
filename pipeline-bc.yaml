apiVersion: v1
kind: BuildConfig
metadata:
  annotations:
    pipeline.alpha.openshift.io/uses: '[{"name": "tasks", "namespace": "", "kind": "DeploymentConfig"}]'
  labels:
    name: tasks-pipeline
  name: tasks-pipeline
spec:
  source: 
    git:
      uri: "http://gogs.apps.ocp4demo.kee.vizuri.com/user-1/openshift-tasks.git"
      ref: "eap-7"
  strategy:
    jenkinsPipelineStrategy:
      env:
      - name: DEV_PROJECT
        value: dev-user0
      - name: STAGE_PROJECT
        value: stage-user0
      jenkinsfilePath: Jenkinsfile
  triggers:
  - github:
      secret: CzgPZAZ5m2
    type: GitHub
  - generic:
      secret: CzgPZAZ5m2
    type: Generic
